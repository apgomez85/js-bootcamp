// === : equality operator
// !== : not equal operator
// !== : not equal operator
// < : less than operator
// > : greater than operator
// <= : less than or equal operator
// >= : greater than or equal operator

let temp = 31;
let isFreezing = temp === 31;

let age = 65;
if (age <= 7) {
  console.log('Receive child discount.');
}

if (age >= 65) {
  console.log('Receive senior discount.');
}
